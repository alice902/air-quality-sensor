#!/usr/bin/env python3

import serial

port = serial
with serial.Serial as port:
	port.baudrate = 9600
	port.open()

print(port.name)				# check which port was really used
port.close()					# close port

